# Enderecos dos contratos apos o deploy local (npx hardhat run scripts/deploy.ts --network localhost).
# Copie para frontend/.env e preencha com os enderecos exibidos no console do deploy.
VITE_TOKEN_ADDRESS=
VITE_VAULT_ADDRESS=
